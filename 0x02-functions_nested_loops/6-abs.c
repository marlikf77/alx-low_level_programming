#include "main.h"
/**
 * _abs - checks for absolute value of integer
 * @c: number to be computed
 * Return: absolute value when success
 */
int _abs(int c)
{
if (c < 0)
{
return (-c);
}
else
{
return (c);
}
}

